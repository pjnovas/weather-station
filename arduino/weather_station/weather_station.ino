/*
 *  Weather Station
 *  To config copy file __config to config, uncomment code and set your configuration
 *
 *  ESP8266 Webserver with a DHT Sensor as input
 *
 *  Webserver
 *   - Serves at root http a JSON with the info of the sensor
 *   - POST to an API the same data every (see in config intervalPOST)
 *
*/
